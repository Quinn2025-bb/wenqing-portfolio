# 业务数据自动化看板 PRD

## 1. 项目背景

蝉妈妈周报推送项目面向业务团队每周查看竞品账号、直播、视频和商品表现的固定需求。过去的工作方式依赖人工进入多个 dashboard，逐页筛选、复制、整理后再同步到周报或飞书消息，流程重复、耗时长，而且容易出现口径不一致、版本分散、漏抓字段等问题。

本项目的目标，是把“人工看数据、人工整理、人工同步”升级成一套可复用的数据产品链路：自动采集平台页面数据，完成清洗和归档，生成可阅读的 HTML 看板，并通过飞书摘要把本周结论快速推送给业务方。

## 2. 产品目标

1. 降低每周整理周报的重复劳动。
2. 让业务负责人能快速看到本周重点变化和下周观察方向。
3. 让运营同学能够继续深入查看明细、趋势和辅助数据。
4. 让项目执行者可以稳定定位失败模块、回溯版本和修复异常。
5. 让指标口径、数据来源和输出版本保持可追溯。

## 3. 用户与场景

| 角色 | 核心诉求 | 典型场景 |
|---|---|---|
| 业务负责人 | 快速判断本周变化和下周重点 | 打开飞书摘要，直接查看本周结论和看板入口 |
| 运营 | 深入查看账号、内容、商品和趋势明细 | 在看板内按业务线、模块和关键词继续看细节 |
| 项目执行 | 稳定跑通采集和周报流程 | 排查失败模块、确认 latest-success、回溯 run_id |
| 数据维护 | 管理指标口径和字段映射 | 更新字段说明、缺失值规则和版本归档方式 |

## 4. 现状问题

- 人工流程跨多个 dashboard，切换成本高。
- 同一类指标在不同模块里存在不同叫法和不同口径。
- 页面经常存在登录态、懒加载、加密接口和同 URL 不同状态等问题。
- 每周整理结果后，只能形成一次性交付，难以沉淀为可复用流程。
- 业务方需要的是结论和重点变化，不只是原始数据堆砌。

## 5. 方案总览

项目采用“浏览器自动化采集 + 数据清洗归档 + HTML 看板 + 飞书摘要推送”的方式，整体链路如下：

```text
业务需求确认
→ 指标与数据源确认
→ 浏览器自动化采集
→ 数据清洗与归一
→ raw / normalized / index 三层归档
→ HTML 周报看板生成
→ 飞书摘要推送
→ 反馈与下一轮迭代
```

看板默认进入周报阅读模式，同时保留探索模式，方便运营继续查看更细的数据。

## 6. 功能需求

### 6.1 P0 功能

- 固定 dashboard 访问入口，检查登录态与页面是否可用。
- 等待页面和模块加载完成后再采集，避免抓到半加载状态。
- 覆盖视频课、图书、APP/单词王三条业务线。
- 采集账号、直播、视频、商品、趋势等核心数据。
- 清洗字段口径，保留原始值和展示值。
- 生成 raw、normalized、index 三层数据归档。
- 输出 RUN_SUMMARY、manifest、latest-success 等运行状态文件。
- 生成 HTML 周报看板。
- 通过飞书推送摘要和看板链接。

### 6.2 P1 功能

- 支持搜索、筛选、排序和业务线切换。
- 支持查看辅助表和原始数据，方便运营继续下钻。
- 支持周报模式与探索模式切换。
- 支持版本回溯，便于查看历史输出。

### 6.3 P2 功能

- 支持权限分层，控制不同角色可见范围。
- 支持异常提醒，覆盖 required 模块失败、登录失效、数据为空等场景。
- 支持后续切换到稳定 API 或内部数据服务接入。

## 7. 数据来源与口径

数据主要来自蝉妈妈等第三方平台的固定 dashboard 页面。当前优先通过浏览器自动化读取前端已解密后的结构化结果，再写入数据池。

### 7.1 核心指标

| 指标 | 默认口径 | 说明 |
|---|---|---|
| 近7日销售额 | 平台当前展示口径 | 保留原始值和展示文案 |
| 销售额环比 | 当前周期相对上一周期 | 用于观察本周变化 |
| 直播销售额 / 销量 | 平台直播模块口径 | 作为直播效果辅助判断 |
| 新发视频表现 | 近7日或平台默认周期 | 用于内容侧观察 |
| 新上架商品 | 平台商品模块口径 | 观察新品和上新动作 |
| 近30日趋势 | 最近30天日维数据 | 用于看整体走势 |

### 7.2 口径原则

- 能拿到原始值时，保留原始值和展示值。
- 页面没提供精确值时，统一保留展示文本，不强行推算。
- 不同业务线的同名字段必须先统一映射，再进入归档。
- 缺失值统一标记为“平台未提供”。

## 8. 核心流程

### 8.1 项目流程

```text
业务方提出需求
→ 确认指标和数据源
→ 设计采集方案
→ 完成数据清洗和归档
→ 生成 HTML 看板
→ 飞书推送结论
→ 反馈问题并进入下一轮迭代
```

### 8.2 自动采集流程

```text
打开固定 dashboard
→ 检查登录态
→ 等待页面 ready
→ 定位真实数据组件
→ 读取 tableData / chartData 等结构化结果
→ 写入 raw 数据
→ 清洗成 normalized 数据
→ 生成运行状态与最新指针
```

## 9. 异常处理

| 异常 | 表现 | 处理方式 |
|---|---|---|
| 登录态失效 | 无法进入目标页面 | 停止执行并提示重新登录 |
| 页面状态不一致 | 同一 URL 显示不同内容 | 先恢复页面基线，再重新采集 |
| 模块懒加载 | 后半段模块抓不到 | 逐步下滚，等模块真正挂载后再取数 |
| 组件定位错误 | 抓到外层卡片而非真实数据层 | 按数据特征重新定位 |
| 字段缺失 | 平台未返回对应字段 | 统一记录为“平台未提供” |
| required 模块失败 | 本轮不能用于正式周报 | 写入 manifest 和 RUN_SUMMARY |

## 10. 交付物

- HTML 周报看板
- 飞书摘要消息
- raw / normalized / index 数据归档
- RUN_SUMMARY
- manifest
- latest-success 指针
- 指标口径说明
- 迭代记录

## 11. 验收标准

1. 能稳定产出一轮结构化结果。
2. 周报默认进入阅读模式，而不是空白筛选页。
3. 看板可支持按业务线查看摘要、趋势和明细。
4. 飞书摘要包含本周结论和 HTML 链接。
5. 任一结果都能回溯到 run_id、week_id 和数据来源。
6. required 模块失败时不会被错误标记为成功。

## 12. 迭代规划

- V1.0：跑通采集、清洗、看板和推送闭环。
- V1.1：补充预览确认和稳定性检查。
- V1.2：增加异常提醒和重试策略。
- V2.0：推进 API 接入，降低对页面状态的依赖。
- V2.1：补充权限管理和多角色看板。
- V2.2：完善指标字典和口径优化机制。
